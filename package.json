{
  "name": "music-cache",
  "private": true,
  "type": "module",
  "version": "1.1.0",
  "description": "DoguIA music proxy: Deezer preview MP3 com cache R2 e truncamento opcional",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "r2:create": "wrangler r2 bucket create music-cache",
    "r2:list": "wrangler r2 object list music-cache",
    "r2:purge": "wrangler r2 bucket delete music-cache --force && npm run r2:create"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
